import {
    __logs,
    __root,
    __templates,
    __uploads,
    relative,
    resolve,
} from '../server/utilities/env.ts';
import { runTask } from '../server/utilities/run-task.ts';

// console.log('Hello world!');
